if(CC)
    add_subdirectory(compiler-c)
endif()
if(CHASKELL)
    add_subdirectory(compiler-haskell)
endif()
if(CGNUASM)
    add_subdirectory(compiler-gnuasm)
endif()
if(CLLVM)
    add_subdirectory(compiler-llvm)
endif()
if(SCOPE)
    add_subdirectory(scope-generator)
endif()
