if ccpp_enabled
  subdir('c')
  subdir('cpar')
endif
if chaskell_enabled
  subdir('haskell')
endif
if cgnuasm_enabled
  subdir('gnuasm')
endif
if cllvm_enabled or jit_enabled or interpreter_enabled
  subdir('llvm')
endif
subdir('pprinter')
