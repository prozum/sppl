if(CPPC)
    add_subdirectory(cpp)
endif()
if(CHASKELL)
    add_subdirectory(haskell)
endif()
if(CGNUASM)
    add_subdirectory(gnuasm)
endif()
if(CLLVM)
    add_subdirectory(llvm)
endif()

add_subdirectory(pprinter)

add_library(codegen)
target_link_libraries(codegen cgnuasm pp)
