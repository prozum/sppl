add_subdirectory(common)
add_subdirectory(parser)
#add_subdirectory(compiler)

#add_executable(scc Main.cpp)
#target_link_libraries(scc common parser compiler)

#install(TARGETS scc
#        RUNTIME DESTINATION bin)
